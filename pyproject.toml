[project]
name = "blammo"
version = "0.1.0"
description = "A Blender add-on"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["cairosvg", "fake-bpy-module-latest", "pillow", "pytest"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
