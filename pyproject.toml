[project]
name = "melvil"
version = "0.1.0"
description = "A Blender add-on"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["fake-bpy-module-latest", "pytest"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
